using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GameStatics
{

    public class StaticFunctions : Singleton<StaticFunctions>
    {
        /*
        public static void SetFocusedCameraToTarget(IFocusedCamera fCamera, Transform target)
        {
            if (fCamera == null)
            {
                Debug.LogError("Focused Camera does not exist.");
                return;
            }
            if (target == null)
            {
                Debug.LogError("Target does not exist.");
                return;
            }

            fCamera.SetFocusedTo(target);
            //Temp, probably move to its own manager or set of functions
        }*/

    }
}